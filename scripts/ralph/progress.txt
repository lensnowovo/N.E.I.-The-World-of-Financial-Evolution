## Codebase Patterns
- MCP 工具鉴权：`app/api/mcp/route.ts` 通过 `getUidFromRequest(req)` 解析 Bearer token（sha256 哈希后比对 `User.mcpTokenHash`），uid 必须通过 per-request 闭包传入 tool handlers，**绝不**用模块级全局变量（否则并发请求会串号）
- 本项目无独立 typecheck script，统一用 `npx tsc --noEmit` 做静态校验；改完代码只需跑这一条
- `createMcpHandler(initializeServer)` 的 `initializeServer` 回调在每次 POST 请求都会被调用（内部为每请求新建 McpServer + transport），所以在 POST/GET/DELETE 入口处按 uid 重建 handler 是安全的，且天然形成闭包隔离
- `sanitizeHtml`（lib/validate.ts）走「白名单标签 + 每标签属性白名单（a.href / img.src / img.alt）+ URL scheme 过滤」；post.body 在写入(POST /api/posts) 与渲染(app/posts/[id]/page.tsx 的 safeBody) 两层都过，改 body 渲染/编辑接口时务必维持此不变量
- rate limiting：所有限流收敛到 `lib/rate-limit.ts` 的 `checkRateLimit(key, limit, windowMs)`（纯内存固定窗口，无依赖，懒过期 + 60s 周期清理）；返回 `retryAfter` 单位是毫秒，写 HTTP Retry-After 头需 Math.ceil(/1000)。key 自带命名空间如 `verify-email:email:${email}`
- 客户端 IP：用 `lib/rate-limit.ts` 的 `getClientIp(req)`（纯 header 解析，无 next 依赖），取 `x-forwarded-for` 第一项，兜底 `x-real-ip`，缺失返回 `'unknown'`（共享桶，比不限流安全）
- rate-limit 维度与限额约定：verify-email = email 3/10min + IP 10/10min；login = email **失败** 5/15min（只在认证失败路径计数，成功登录不计）；register / reset-password = IP 10/10min。429 响应统一带 `Retry-After` 头（秒）+ body `{ error, retryAfter }`
- OAuth/CSRF state：收敛到 `lib/oauth-state.ts` 的 `issueOAuthState()`/`verifyOAuthState()`（HMAC 签名 token 复用 SESSION_SECRET，短期 one-shot cookie `pevc_oauth_state`，SameSite=Lax）。需要做 CSRF 防护的第三方跳转回跳场景都走这套
- 开放重定向防护：客户端/服务端拿 `?next=` 做跳转前必须校验「单个 `/` 开头 && 非 `//` && 不含 `\`」。`//evil.com` 是协议相对 URL，浏览器会当 https://evil.com；部分浏览器会把 `/\` 规范化成 `//`，所以反斜杠也要拒。不合规一律回退 `/`
- 用户枚举模糊：登录/重置/发码等鉴权接口，账号不存在时不能返回 "账号不存在"/"未注册"，必须返回与「凭证错误」完全一致的文案 + 状态码（如密码模式 → "邮箱或密码错误" 401；验证码模式 → "验证码无效或已过期" 400）。否则攻击者可枚举探测已注册邮箱

---

## 2026-06-24 (init) - US-001
- 移除 `app/api/mcp/route.ts` 中模块级 `let currentUid: number | null = null` 全局变量（race condition 根源）
- 引入 `makeHandler(uid: number)` 工厂：每次请求由 POST/GET/DELETE 解析 token 得到 uid 后，新建一个闭包捕获 uid 的 handler；三个 tool（search_skills/get_skill/list_my_skills）全部改为从闭包读 uid，`list_my_skills` 的 `where` 用 `userId: uid`
- 新增 `unauthorizedResponse()` 助手，三个 HTTP method 入口保留 401 早返回
- 更改的文件：app/api/mcp/route.ts
- 类型检查：`npx tsc --noEmit` 通过（exit 0）
- 浏览器/并发验证（acceptanceCriteria 第 5 条）：本地无 DATABASE_URL 无法跑实际并发请求，需部署后人工验证
- **未来迭代的学习：**
  - 陷阱：写对象 shorthand `where: { userId }` 时 TS strict 会报 TS18004（"No value exists in scope for the shorthand property"），因为函数参数叫 `uid` 不叫 `userId`；要么显式 `userId: uid`，要么把参数命名为 userId
  - pattern：mcp-handler 库内部每请求都新建 McpServer + transport 并调用 `initializeServer`，因此 per-request 闭包是解决 MCP 并发隔离的标准姿势，不需要 AsyncLocalStorage
  - 上下文：`User.mcpTokenHash` 字段存 token 的 sha256 hex；token 以 `nei_` 前缀开头，明文不入库
---
## 2026-06-24 17:12 - US-002
- 重写 lib/validate.ts 的 sanitizeHtml：
  - 整块移除 script/style/iframe/object/embed/svg/math/template/noscript/form/frame/applet/head（含内部内容）+ 未闭合的孤立开标签兜底
  - 移除 HTML 注释 / CDATA / PI / doctype（IE 条件注释等注入面）
  - 逐标签重建：未知标签整段丢弃；已知标签按白名单 a.href / img.src / img.alt 保留属性，其余（含所有 on* 事件处理器，无论引号形式）一律剥离
  - href/src 过滤：解码 HTML 实体 + 去空白后拒绝 javascript:/vbscript:/data: scheme（含大小写/实体/前导控制字符混淆）
  - 输出属性值重新 escapeAttr，避免二次注入
- 在 app/posts/[id]/page.tsx 渲染路径加 defense-in-depth：fetch post 后 safeBody = sanitizeHtml(post.body)，excerpt / preSplit(promptParts) / 非提示词正文渲染(原 line 199)均基于 safeBody；提示词 part.html 因派生自 preSplit 也自动安全。CopyPromptButton/PreCopyButton 的 bodyHtml 仍是 post.body（仅 regex 提取文本复制，不渲染 HTML，无 XSS 面，故保留原值）
- 更改的文件：lib/validate.ts、app/posts/[id]/page.tsx
- 类型检查：`npx tsc --noEmit` 通过（EXIT=0）
- 浏览器验证（acceptanceCriteria 第 5 条）：本地无 DATABASE_URL 无法跑实际渲染，需部署后人工验证。已在脑内过 6 类 payload：<img src=x onerror=alert(1)> / <a href=javascript:...> / 实体混淆 / data: / 注释 / 大小写，均被中和
- **未来迭代的学习：**
  - 陷阱：原 sanitizeHtml 用 `\son\w+="..."` 只覆盖引号形式的 on*，且 `javascript:` 全文 replace 会误伤正文里出现的字面量 "javascript:" 文本；正确姿势是「属性白名单」（不在白名单一律丢）而非「黑名单剥离」
  - 陷阱：TAG_RE 用 `[^>]*` 匹配属性段，遇到属性值里含 `>` 会过早截断 —— 但这对安全是「向更保守方向」偏差（解析到的属性更少），不会产生绕过；相反，浏览器解析可能看到更多，所以白名单 + 重建比黑名单更稳
  - pattern：dangerouslySetInnerHTML 的内容必须在「写入时」和「渲染时」至少一处过 sanitizeHtml；本次两层都过（POST /api/posts 写入时 + page.tsx 渲染时），覆盖 DB legacy 数据
  - 上下文：post.body 的下游消费者：page.tsx 正文 dangerouslySetInnerHTML（需 sanitize）、excerpt 标签剥离（无 XSS 面）、preSplit/promptParts（派生自 body，渲染时用 dangerouslySetInnerHTML）、CopyPromptButton/PreCopyButton 仅 regex 提取文本（无 XSS 面）
---
## 2026-06-24 17:30 - US-003
- 新增 `lib/secret.ts`：集中处理 SESSION_SECRET 解析。生产环境（NODE_ENV==='production'）下未设置或仍为默认值 `dev-secret-change-me` 时在 module load 即抛错；开发环境回退默认值
- `lib/session.ts` 与 `lib/crypto.ts` 删除各自 `process.env.SESSION_SECRET || 'dev-secret-change-me'`，改为 `import { SESSION_SECRET } from './secret'`，共享同一份带守卫的 secret
- 更改的文件：lib/secret.ts（新增）、lib/session.ts、lib/crypto.ts
- 类型检查：`npx tsc --noEmit` 通过（EXIT=0）
- **未来迭代的学习：**
  - pattern：全局密钥/单例类常量应收敛到一个 `lib/secret.ts`（或类似 module），并在生产环境做 fail-fast；避免每个文件各自 `process.env.X || '默认'`（复制粘贴 = 漏校验）
  - 陷阱：在 Next.js 中，route handler / server component 首次被 import 时即触发 module-level 代码执行；所以 `lib/secret.ts` 顶层的 `resolveSecret()` 会在任何生产请求 handler 加载时立即抛出，无需额外 hook（fail-fast 自然生效）
  - 上下文：SESSION_SECRET 同时用于 (a) session cookie 的 HMAC 签名（lib/session.ts）(b) API-key AES-256-GCM 密钥派生（lib/crypto.ts 经 scrypt）；任一被泄露/默认化都会让用户会话和加密的 LLM 凭据都可伪造
---

## 2026-06-24 17:45 - US-004
- 新增 lib/rate-limit.ts：纯内存固定窗口 rate limiter，无运行时依赖
  - 导出 checkRateLimit(key, limit, windowMs) -> { ok, retryAfter }
  - 懒过期：命中时若 expiresAt <= now 重开窗口
  - 周期清理：每 60s 最多跑一次 pruneStale，避免 Map 无限增长
  - retryAfter 单位为毫秒；调用方写 HTTP Retry-After 头自行 Math.ceil(/1000)
  - 语义：limit=N 表示允许 N 次，第 N+1 次被拒（满足 US-005 中「3 次后第 4 次 429」需求）
- 更改的文件：lib/rate-limit.ts（新增）
- 类型检查：`npx tsc --noEmit` 通过（EXIT=0）
- **未来迭代的学习：**
  - pattern：rate-limit 是纯函数 + 模块级 Map 单例；多实例/serverless 下不共享。Sprint 0.x 基础防护足够，未来跨实例一致限流换 Redis
  - pattern：固定窗口实现最简，首请求建桶 + 计数 + 过期重置；调用方按 (key, limit, windowMs) 三元组自行决定维度（email/IP/uid）
  - 上下文：US-005 会调用此 helper，verify-email 用 email 维度(limit 3 / 10min) + IP 维度(limit 10 / 10min)；login 用 email 维度(limit 5 / 15min)。HTTP 429 响应需设 Retry-After 头（秒）
  - 陷阱：拒绝路径不要继续递增 count，否则窗口永远不会自然重置后允许新请求；只有 ok=true 时才 count+=1
---

## 2026-06-24 18:05 - US-005
- lib/rate-limit.ts 新增 `getClientIp(req)`：纯 header 解析（x-forwarded-for 第一项 → x-real-ip → 'unknown'），不引入 next/server 依赖，保持模块框架无关
- verify-email：加 email（3/10min）+ IP（10/10min）双维度限流，任一命中即 429；key 用 `email.toLowerCase()` 防大小写绕过；两个维度并行检查（都计数一次请求），retryAfter 取两者最大值
- login：加 email **失败计数** 限流（5 失败/15min）；用闭包 `onAuthFail()` 在「用户不存在 / 验证码错 / 未设密码 / 密码错」四个认证失败路径调用，命中上限返回 429（通用文案）；**成功登录与输入格式校验失败均不计入**（前者是正常行为，后者算 client 错误）
- register / reset-password：加 IP 级别限流（10/10min），在业务校验前早返回
- 所有 429 响应统一：status 429 + `Retry-After` 头（秒）+ body `{ error, retryAfter }`
- 更改的文件：lib/rate-limit.ts、app/api/auth/verify-email/route.ts、app/api/auth/login/route.ts、app/api/auth/register/route.ts、app/api/auth/reset-password/route.ts
- 类型检查：`npx tsc --noEmit` 通过（EXIT=0）
- 浏览器验证（acceptanceCriteria 第 5 条「verify-email 4 次第 4 次 429」）：本地无 DATABASE_URL 无法跑实际 HTTP，需部署后人工验证。已脑演：同 email 第 4 次请求时 email bucket count=3，`3 < 3` 为 false → ok=false → 429 ✓；IP 维度 4 < 10 不触发
- **未来迭代的学习：**
  - pattern：「仅计数失败」语义不能用 checkRateLimit 的「检查即递增」直接套 —— 必须把调用放在失败路径上（login 的 onAuthFail 闭包模式），否则成功请求也会被计入导致误杀正常用户
  - pattern：多维度限流（如 email + IP）应「都检查、都计数」，retryAfter 取最大值；不要 short-circuit（用 `||` 求值会让第二个维度不递增，但本次显式求值两次确保都计数）
  - 陷阱：email 作为限流 key 必须 `toLowerCase()`，否则 `a@x.com` 和 `A@x.com` 是两个独立桶，等于限额翻倍
  - 陷阱：`Retry-After` 头单位是秒，body 里 `retryAfter` 也用秒保持一致；从 checkRateLimit 的毫秒转换用 `Math.max(1, Math.ceil(/1000))` 保证至少 1 秒
  - 上下文：login 的「用户不存在」返回 404 + 明确文案（账号不存在）会泄露用户是否注册，US-007 会把它改成与密码错不可区分的通用错误；本 story 不动文案，只在失败路径挂限流
---

## 2026-06-24 17:55 - US-006
- 新增 lib/oauth-state.ts：HMAC 签名的 one-shot OAuth state token（payload = {nonce, exp}，base64url(payload).base64url(hmac)），签名 key 复用 SESSION_SECRET（与 session/crypto 同一密钥派生链）
  - `issueOAuthState()`：生成随机 nonce + 10min 过期，写短期 cookie（pevc_oauth_state，httpOnly + sameSite=lax + secure in prod），返回 token
  - `verifyOAuthState(queryState)`：先清 cookie（one-shot 防重放）；queryState 与 cookie 必须严格相等（crypto.timingSafeEqual 常量时间比较）；再校验签名 + 过期
- app/api/auth/github/route.ts：发起 OAuth 时先 `const state = await issueOAuthState()`，把 `&state=${encodeURIComponent(state)}` 拼到 GitHub authorize URL
- app/api/auth/github/callback/route.ts：入口最先调用 `verifyOAuthState(req.nextUrl.searchParams.get('state'))`，失败直接 redirect `/login?error=invalid_state`，code 校验移到 state 之后
- 更改的文件：lib/oauth-state.ts（新增）、app/api/auth/github/route.ts、app/api/auth/github/callback/route.ts
- 类型检查：`npx tsc --noEmit` 通过（EXIT=0）
- 浏览器验证（acceptanceCriteria 第 4 条「篡改 state 被拒」）：本地无 DATABASE_URL 但 OAuth 不依赖 DB，理论上可起 dev 跑；但 GitHub OAuth 需真实 GITHUB_CLIENT_ID + redirect 配置，本地不便完整跑。需部署后人工验证。已脑演：(a) 正常流程 — issue 写 cookie + 拼 state，回调时 query.state === cookie.value 且签名有效 → 通过；(b) 篡改 state — query 与 cookie 不等 → timingSafeEqual 失败 → redirect /login?error=invalid_state ✓；(c) 攻击者构造请求无 cookie — cookie 缺失 → false → invalid_state ✓；(d) 旧 state 重放 — cookie 已被上次 verify 清掉 → false ✓
- **未来迭代的学习：**
  - pattern：OAuth/CSRF state 的「签名 + cookie↔query 配对」是标准姿势 —— 签名只证明 state 来自本服务器，相等比较才证明浏览器拿到的就是本服务器发出的那份（防 CSRF 攻击者凭空构造一个合法 state）。两层都要有
  - pattern：state cookie 必须是 `sameSite: 'lax'`（GitHub 回调是顶级 GET 导航，lax 会被发送；strict 会被拒导致 OAuth 永远失败）。httpOnly 防止 JS 偷读。短 maxAge + verify 后立即清掉 → 一次性，防重放
  - pattern：HMAC 签名与 session cookie 同密钥派生链（SESSION_SECRET）是安全的 —— 两者的「用途域」不重叠（session 是长期身份 cookie，oauth state 是一次性 CSRF token），且密钥已通过 US-003 做了生产 fail-fast 守卫
  - 陷阱：`crypto.timingSafeEqual` 要求两个 Buffer 等长，否则抛错；先 `if (a.length !== b.length) return false` 再比较，避免异常路径泄露长度信息
  - 陷阱：cookie 的 `maxAge: 0` 是「立即删除」的标准姿势（不是「session cookie」）；写空值 + maxAge=0 双保险，适配不同浏览器
  - 上下文：US-007 会顺带处理 login redirect 校验 / verify-code CSPRNG / 用户枚举模糊 —— 本 story 只动 github OAuth 两条路由，不动 login UI 和 verify-email 路由
---

## 2026-06-24 19:40 - US-007
- app/login/page.tsx：`next` 查询参数加白名单校验 —— 仅允许「单个 `/` 开头 && 非 `//` 开头 && 不含 `\`」，否则回退 `/`。防止 `?next=//evil.com`（协议相对 → 跳出站外）与 `?next=/\evil.com`（反斜杠被部分浏览器规范化为 `//`）两类开放重定向
- app/api/auth/verify-email/route.ts：验证码生成从 `Math.floor(100000 + Math.random() * 900000)` 改为 `crypto.randomInt(100000, 1000000)`（node:crypto 的 CSPRNG，返回 [100000, 1000000) 区间整数，即 6 位数字）。Math.random() 非密码学安全，理论上可被预测
- app/api/auth/login/route.ts：账号不存在时不再返回 `{ error: '账号不存在' }`（404，明示邮箱未注册），改为按当前 `mode` 返回与凭证失败一致的响应：code 模式 → `'验证码无效或已过期'` (400)，password 模式 → `'邮箱或密码错误'` (401)。状态码也匹配，使响应在文案 + 状态码两个维度都无法区分「账号不存在」与「凭证错」
- app/api/auth/reset-password/route.ts：账号不存在时不再返回 `'该邮箱未注册'` (404)，改为 `'验证码无效或已过期'` (400)，与验证码无效路径完全一致
- 更改的文件：app/login/page.tsx、app/api/auth/verify-email/route.ts、app/api/auth/login/route.ts、app/api/auth/reset-password/route.ts
- 类型检查：`npx tsc --noEmit` 通过（EXIT=0）
- 浏览器验证（acceptanceCriteria 第 5 条「?next=//evil.com 重定向到 /」）：本地无 DATABASE_URL 但该验收纯客户端逻辑（useSearchParams → router.push），不依赖 DB。需部署后人工验证。已脑演：(a) `?next=//evil.com` → startsWith('//')=true → 回退 `/` ✓；(b) `?next=/\evil.com` → includes('\')=true → 回退 `/` ✓；(c) `?next=/posts/123` → 合规站内路径 → 跳转 /posts/123 ✓；(d) `?next=https://evil.com` → 非 `/` 开头 → 回退 `/` ✓；(e) `?next=` 缺失 → 默认 `/` ✓
- 额外脑演用户枚举模糊：登录 a@x.com（不存在）密码模式 → 401 "邮箱或密码错误"，与 b@x.com（存在）密码错返回完全一致 ✓；reset-password 对不存在邮箱 → 400 "验证码无效或已过期"，与验证码错一致 ✓
- **未来迭代的学习：**
  - pattern：开放重定向校验三件套「单 `/` 开头 && 非 `//` && 不含 `\`」。只查 `//` 不够 —— Windows/老版 Chrome 会把 `/\` 当 `//`；只查前缀 `//` 也不够，`/\evil.com` 的 startsWith('//') 是 false 但同样危险。所以用 `includes('\')` 兜底所有反斜杠
  - pattern：用户枚举模糊必须同时对齐「文案 + HTTP 状态码」两个维度。只改文案不改状态码（如账号不存在 404、密码错 401）依然可通过状态码枚举。本次 login 按模式分别对齐到 400/401，reset-password 对齐到 400
  - 陷阱：`crypto.randomInt(min, max)` 是「左闭右开」[min, max)，要拿 6 位数字（100000-999999）必须写 `randomInt(100000, 1000000)`，写 `randomInt(100000, 999999)` 会漏掉 999999（虽然对验证码无实际影响，但语义错）
  - 陷阱：login 路由的 user 查找在 mode 分支之前，所以「账号不存在」时拿不到 mode 的后续校验信息（如 code 格式）。本次按 mode 返回对应通用错误已足够；若未来要完全消除时序差异（账号不存在时不应比凭证错误更快返回），需在不存在账号时也跑一次假的 bcrypt.compare —— 本 story 不做，US-005 的 rate limit 已是主要防护层
  - 上下文：US-008 会新增 User.isAdmin 字段，getCurrentUser select 会扩展 —— 本 story 未动 schema，无 prisma generate 步骤
  - 上下文：github/callback 路由的重定向是硬编码 `new URL('/', req.url)`（line 96），不走 `next` 参数，故无需校验；仅 app/login/page.tsx 一处消费 `next`
---
